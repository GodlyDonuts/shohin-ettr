# PCF7: Ministral Publication Confirmation Successor

Status: prospectively frozen on 2026-08-11 before PCF7 admission or compute.

PCF1 through PCF6 remain closed with formal scientific result `null`. PCF6's
no-model H100 environment canary removed `SLURM_EXPORT_ENV` as intended, then
the actual-variable scan stopped on Bash's `_` variable. For an external
command Bash exports `_` as the invoked executable path; here it was exactly
the already-pinned Python entrypoint. The canary stopped before `nvidia-smi`,
CUDA, model access, output publication, or scientific graph submission.
Preserve `SHOHIN_PCF6_TERMINAL_INFRASTRUCTURE_RECEIPT_20260811.json`.

PCF7 is the separately named, prospectively declared infrastructure successor
authorized by the user's standing direction to continue fixing pre-science
blockers. It inherits every scientific and security clause of PCF1 through
PCF6. The host, model, source bytes, split, prompts, arms, seeds, training
geometry, candidate policy, thresholds, assessor custody, and terminal gate
remain unchanged.

Its sole repair is one exact control-plane alias: `_` may contain a protected
term only when both its name is `_` and its complete value equals the same
hash-pinned Python entrypoint already admitted for `PYTHON`. Every other `_`
value fails. `SLURM_EXPORT_ENV` remains removed, and every other actual name
and value remains subject to the unchanged protected-term scan.

Run one fresh no-model H100 environment canary. It must prove export metadata
present before and absent after filtering, the exact actual environment
accepted, an eligible H100 allocation, and zero model/scientific access. If it
passes, preserve its receipt, replay the full live preflight, and submit one
PCF7 graph. Disable requeue, retries, and automatic successors. Any
infrastructure failure, formal `PASS`, or formal `FAIL` ends PCF7. Do not
change or open an alternate host, data split, prompt, arm, seed, threshold, or
protected board.
