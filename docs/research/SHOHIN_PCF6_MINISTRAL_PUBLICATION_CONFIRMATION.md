# PCF6: Ministral Publication Confirmation Successor

Status: prospectively frozen on 2026-08-11 before PCF6 admission or compute.

PCF1 through PCF5 remain closed with formal scientific result `null`. PCF5
passed preparation, then its H100 mechanics job stopped after one second,
before runtime verification, prepared-input binding, model verification,
scratch creation, CUDA, or model load. Slurm automatically inserted
`SLURM_EXPORT_ENV`; that metadata value duplicated the entire export payload
and therefore contained the hash-pinned historical Python directory name
`product-reasoning`. The GPU firewall rejected the metadata string. All 27
never-started descendants were cancelled, the queue was emptied, and the
prepared assessor remained sealed with zero semantic reads. Preserve
`SHOHIN_PCF5_TERMINAL_INFRASTRUCTURE_RECEIPT_20260811.json`.

The user has explicitly directed continued pre-science infrastructure repair
in pursuit of Shohin. PCF6 is the separately named, prospectively declared
successor. It inherits every scientific and security clause of PCF1 through
PCF5. The host, model revision, source bytes and hashes, split, prompts, arms,
seeds, training geometry, candidate policy, thresholds, single assessor open,
and conjunctive terminal gate are unchanged.

Its sole repair is GPU environment projection. `SLURM_EXPORT_ENV` is
Slurm-generated control-plane metadata that repeats `--export`; it is removed
before any child process. Every actual environment variable and value remains
present and is scanned under the unchanged protected-term firewall, including
the sole exact Python-entrypoint exception. Removing the duplicate metadata
does not admit or suppress any actual data, model, output, assessor, holdout,
product, or public variable.

Before a graph, run exactly one no-model H100 environment canary. It must prove
that `SLURM_EXPORT_ENV` existed before filtering and is absent afterward, that
the complete actual GPU environment passed the firewall, that the allocation
is one eligible H100 on `normal`, and that no model or score was opened. If the
canary passes, preserve its receipt, replay the full live preflight, and submit
one PCF6 graph. Disable requeue, retries, and automatic successors. Any
infrastructure failure, formal `PASS`, or formal `FAIL` ends PCF6. Do not
change or open an alternate host, data split, prompt, arm, seed, threshold, or
protected board.
